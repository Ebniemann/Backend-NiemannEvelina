<div class="container mt-5 mb-5">
  <div class="row justify-content-center">
    <div class="col-lg-4">
      <div class="card">
        <div class="card-header">
          <h3 class="text-center">Login</h3>
        </div>

        <div class="card-body">
          <form id="loginForm" action="/api/sessions/login" method="post">

            {{#if error}}
              <div class="alert alert-danger" role="alert">
                {{error}}
              </div>
            {{/if}}
            <!-- Mostrar mensajes -->
            {{#if mensaje}}
              <div class="alert alert-success" role="alert">
                {{mensaje}}
              </div>
            {{/if}}

            <a class="btn bg-dark btn-block mb-1" href="/api/sessions/github">
              <i class="fab fa-github"></i>
              Acceso con GitHub
            </a>

            <div class="form-group">
              <label for="email">Email</label>
              <input
                type="email"
                name="email"
                id="email"
                class="form-control"
                placeholder="Ingresa tu email"
                required
              />
            </div>
            <div class="form-group">
              <label for="password">Password</label>
              <input
                type="password"
                id="password"
                name="password"
                class="form-control"
                placeholder="Ingresa tu clave"
                required
              />
            </div>

            <div class="text-center mb-1 mt-2">
              <a href="/signup" class="forgot-password-link">¿Aun no estás
                registrado?</a>
            </div>

            <div class="text-center mb-3">
              <a
                href="/password-recovery"
                class="forgot-password-link"
              >¿Olvidaste tu contraseña?</a>
            </div>

            <button
              type="submit"
              class="btn btn-primary btn-block"
            >Login</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>
{{! 
<h2>Login Usuario:</h2>
<hr />
<form action="/api/sessions/login" method="post">
  <a href="/api/sessions/github">Acceso con GitHub</a>

  <br />
  <br />

  <input type="email" name="email" placeholder="Email" /><br /><br />
  <input type="text" name="password" placeholder="Contraseña" />
  <br />
  <br />

  <input type="submit" value="Iniciar Sesión" />
</form>

<br /> }}

{{! <a href="/password-recovery">Recuperar clave</a> }}